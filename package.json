{
  "name": "root",
  "version": "1.0.0",
  "description": "Mi primer pagina web con sass.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-ess": "node-sass --include-path sess sess/style.sess css/main.css",
    "watch-css": "nodemon -e sess -x \"npm run build-ess\""
  },
  "keywords": [
    "developer"
  ],
  "author": "Matias Miranda Otero",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
